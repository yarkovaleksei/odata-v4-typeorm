# Результаты сборки и установки — форматировать нечего.
node_modules
build
coverage
examples/server/public/js

# Файл данных фикстур: выравнивание в нём смысловое (колонки INSERT читаются
# как таблица), и Prettier для SQL всё равно ничего не делает.
src/test/fixtures/seed.sql

# Коллекция Postman — машинно сгенерированный JSON, переформатирование даст
# бессмысленный diff при каждой перегенерации.
examples/postman

# Документация. Prettier здесь занимался бы только выравниванием таблиц и переносами
# прозы — на корректность это не влияет, зато один прогон переписал бы все восемь
# документов целиком. У них своя договорённость: 100 колонок и таблицы, собранные руками.
# Целостность документации проверяет отдельный шаг CI (`yarn docs:check`).
*.md
